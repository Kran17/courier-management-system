<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Edit Delivery</title>
    <style>
        body {
            padding: 20px;
        }
        form {
            max-width: 500px;
            margin: 0 auto;
        }
        .form-group {
            margin-bottom: 20px;
        }
        label {
            font-weight: bold;
        }
        button1[type="submit"] {
            background-color: #007bff;
            color: #fff;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            border-radius: 5px;
        }
        button1[type="submit"]:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body data-bs-theme ="dark">
    <h1 class="text-center mb-4">Edit Delivery</h1>
    <form action="/editDelivery" method="POST" class="container">
        <input type="hidden" name="deliveryId" value="<%= delivery._id %>">
        
        <div class="row mb-3">
            <label for="packagename" class="col-sm-3 col-form-label">Package Name:</label>
            <div class="col-sm-9">
                <input type="text" id="packagename" name="packagename" value="<%= delivery.packagename %>" class="form-control">
            </div>
        </div>
    
        <div class="row mb-3">
            <label for="package_description" class="col-sm-3 col-form-label">Package Description:</label>
            <div class="col-sm-9">
                <input type="text" id="package_description" name="package_description" value="<%= delivery.package_description %>" class="form-control">
            </div>
        </div>
    
        <div class="row mb-3">
            <label for="package_weight" class="col-sm-3 col-form-label">Package Weight (in kg):</label>
            <div class="col-sm-9">
                <input type="number" id="package_weight" name="package_weight" value="<%= delivery.package_weight %>" class="form-control">
            </div>
        </div>
    
        <!-- Add more form groups similarly -->
    
        <div class="row mb-3">
            <label for="service_type" class="col-sm-3 col-form-label">Service Type:</label>
            <div class="col-sm-9">
                <select id="service_type" name="service_type" class="form-select">
                    <option value="standard" <% if (delivery.service_type === 'standard') { %>selected<% } %>>Standard</option>
                    <option value="express" <% if (delivery.service_type === 'express') { %>selected<% } %>>Express</option>
                </select>
            </div>
        </div>
    
        <div class="row mb-3">
            <label for="pickup_time" class="col-sm-3 col-form-label">Preferred Pickup Time:</label>
            <div class="col-sm-9">
                <input type="datetime-local" id="pickup_time" name="pickup_time" value="<%= delivery.pickup_time %>" class="form-control">
            </div>
        </div>
    
        <div class="row mb-3">
            <label for="delivery_time" class="col-sm-3 col-form-label">Preferred Delivery Time:</label>
            <div class="col-sm-9">
                <input type="datetime-local" id="delivery_time" name="delivery_time" value="<%= delivery.delivery_time %>" class="form-control">
            </div>
        </div>
    
        <div class="row">
            <div class="col text-center">
                <button type="submit" class="btn btn-outline-success">Save Changes</button>
            </div>
        </div>
    </form>
    
    
</body>
</html>

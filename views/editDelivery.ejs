<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <title>Edit Delivery</title>
    <style>
        body {
            padding: 20px;
        }
        form {
            max-width: 500px;
            margin: 0 auto;
        }
        .form-group {
            margin-bottom: 20px;
        }
        label {
            font-weight: bold;
        }
        button1[type="submit"] {
            background-color: #007bff;
            color: #fff;
            border: none;
            padding: 10px 20px;
            cursor: pointer;
            border-radius: 5px;
        }
        button1[type="submit"]:hover {
            background-color: #0056b3;
        }
    </style>
</head>
<body data-bs-theme ="dark">
    <h1 class="text-center mb-4">Edit Delivery</h1>
    <form action="/editDelivery" method="POST">
        <input type="hidden" name="deliveryId" value="<%= delivery._id %>">
        <div class="form-group">
            <label for="packagename" class="form-label">Package Name:</label>
            <input type="text" id="packagename" name="packagename" value="<%= delivery.packagename %>" class="form-control">
        </div>
        <div class="form-group">
            <label for="package_description" class="form-label">Package Description:</label>
            <input type="text" id="package_description" name="package_description" value="<%= delivery.package_description %>" class="form-control">
        </div>
        <div class="form-group">
            <label for="package_weight" class="form-label">Package Weight (in kg):</label>
            <input type="number" id="package_weight" name="package_weight" value="<%= delivery.package_weight %>" class="form-control">
        </div>
        <div class="form-group">
            <label for="sender_name" class="form-label">Sender Name:</label>
            <input type="text" id="sender_name" name="sender_name" value="<%= delivery.sender_name %>" class="form-control">
        </div>
        <div class="form-group">
            <label for="sender_address" class="form-label">Sender Address:</label>
            <input type="text" id="sender_address" name="sender_address" value="<%= delivery.sender_address %>" class="form-control">
        </div>
        <div class="form-group">
            <label for="sender_contact" class="form-label">Sender Contact:</label>
            <input type="tel" id="sender_contact" name="sender_contact" value="<%= delivery.sender_contact %>" class="form-control">
        </div>
        <div class="form-group">
            <label for="receiver_name" class="form-label">Receiver Name:</label>
            <input type="text" id="receiver_name" name="receiver_name" value="<%= delivery.receiver_name %>" class="form-control">
        </div>
        <div class="form-group">
            <label for="receiver_address" class="form-label">Receiver Address:</label>
            <input type="text" id="receiver_address" name="receiver_address" value="<%= delivery.receiver_address %>" class="form-control">
        </div>
        <div class="form-group">
            <label for="receiver_contact" class="form-label">Receiver Contact:</label>
            <input type="tel" id="receiver_contact" name="receiver_contact" value="<%= delivery.receiver_contact %>" class="form-control">
        </div>
        <div class="form-group">
            <label for="service_type" class="form-label">Service Type:</label>
            <select id="service_type" name="service_type" class="form-select">
                <option value="standard" <% if (delivery.service_type === 'standard') { %>selected<% } %>>Standard</option>
                <option value="express" <% if (delivery.service_type === 'express') { %>selected<% } %>>Express</option>
            </select>
        </div>
        
        <div class="form-group">
            <label for="pickup_time" class="form-label">Preferred Pickup Time:</label>
            <input type="datetime-local" id="pickup_time" name="pickup_time" value="<%= delivery.pickup_time %>" class="form-control">
        </div>
        <div class="form-group">
            <label for="delivery_time" class="form-label">Preferred Delivery Time:</label>
            <input type="datetime-local" id="delivery_time" name="delivery_time" value="<%= delivery.delivery_time %>" class="form-control">
        </div>
        <div class="text-center">
            <button type="submit" class="btn btn-outline-success">Save Changes</button>
        </div>
    </form>
    
</body>
</html>

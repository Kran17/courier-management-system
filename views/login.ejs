<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Login Page</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="styles.css">
</head>
<body>

    <div class="main">  	
        <input type="checkbox" id="chk" aria-hidden="true">
        

            <div class="login">
                <form class="form" id="loginForm" action="/login" method="post">
                    <label for="chk" aria-hidden="true">Log in</label>
                    <input class="input" type="text" name="username" id="password" placeholder="Email" required="">
                    <input class="input" type="password" name="password"id="password" placeholder="Password" required="">
                    <button>Log in</button>
                </form>
            </div>

            <div class="register">
                <form class="form">
                    <label for="chk" aria-hidden="true">Register</label>
                    <input class="input" type="text" name="username" id="username"placeholder="Username" required="">
                    
                    <input class="input" type="password" name="password" id="password"placeholder="Password" required="">
                    <button>Register</button>
                </form>
            </div>

            
    </div>
    <div class="additional-content">
        <h1 style="color: #520898;"><b>WayWise</b></h1>
    <h1>Get your shipment picked up</h1>
    <h1>    from your doorstep</h1>
    <h3>  Send your parcel from the comfort of your home or office</h3></div>
    

    <script>
        document.addEventListener("DOMContentLoaded", function() {
            const usernameInput = document.getElementById("username");
            const passwordInput = document.getElementById("password");
            const usernameError = document.getElementById("usernameError");
            const passwordError = document.getElementById("passwordError");
            const loginButton = document.getElementById("loginButton");

            // Function to validate the username field
            function validateUsername() {
                const username = usernameInput.value.trim();
                const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
                const phonePattern = /^\d{10}$/;
                if (!emailPattern.test(username) && !phonePattern.test(username)) {
                    usernameError.textContent = "Please enter a valid email or phone number.";
                    return false;
                } else {
                    usernameError.textContent = ""; // Clear any previous error message
                    return true;
                }
            }

            // Function to validate the password field
            function validatePassword() {
                const password = passwordInput.value.trim();
                if (password.length < 8) {
                    passwordError.textContent = "Password must be at least 8 characters long.";
                    return false;
                } else {
                    passwordError.textContent = ""; // Clear any previous error message
                    return true;
                }
            }

            // Event listeners for input events on username and password fields for real-time validation
            usernameInput.addEventListener("input", function() {
                validateUsername();
                // Enable or disable login button based on validation result
                loginButton.disabled = !validateUsername() || !validatePassword();
            });

            passwordInput.addEventListener("input", function() {
                validatePassword();
                // Enable or disable login button based on validation result
                loginButton.disabled = !validateUsername() || !validatePassword();
            });
        });
    </script>
    <script>
        document.addEventListener("DOMContentLoaded", function() {
    const usernameInput = document.getElementById("username");
    const passwordInput = document.getElementById("password");
    const usernameError = document.getElementById("usernameError");
    const passwordError = document.getElementById("passwordError");
    const loginButton = document.querySelector(".login button");

    // Function to validate the username field
    function validateUsername() {
        const username = usernameInput.value.trim();
        const emailPattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
        const phonePattern = /^\d{10}$/;
        if (!emailPattern.test(username) && !phonePattern.test(username)) {
            usernameError.textContent = "Please enter a valid email or phone number.";
            return false;
        } else {
            usernameError.textContent = ""; // Clear any previous error message
            return true;
        }
    }

    // Function to validate the password field
    function validatePassword() {
        const password = passwordInput.value.trim();
        if (password.length < 8) {
            passwordError.textContent = "Password must be at least 8 characters long.";
            return false;
        } else {
            passwordError.textContent = ""; // Clear any previous error message
            return true;
        }
    }

    // Event listeners for input events on username and password fields for real-time validation
    usernameInput.addEventListener("input", function() {
        validateUsername();
        // Enable or disable login button based on validation result
        loginButton.disabled = !validateUsername() || !validatePassword();
    });

    passwordInput.addEventListener("input", function() {
        validatePassword();
        // Enable or disable login button based on validation result
        loginButton.disabled = !validateUsername() || !validatePassword();
    });
});

    </script>
    <script>
        const burgerMenu = document.querySelector('.burger-menu');
        const navMenu = document.querySelector('.nav-menu');
        burgerMenu.addEventListener('click', () => {
        navMenu.classList.toggle('active');
        });
    </script>
</body>
</html>
